# خطأ مؤكّد: طلب مخزون الرزبشن لا يظهر في موافقات الأدمن (نفس العيادة)

## إعادة الإنتاج (على الإنتاج، اليوم)
1. `reception@example.com` (رزبشن) أرسل طلب مخزون:
   - `POST /inventory/requests` body `{submit:true, items:[{inventory_item_id:17, requested_qty:3}], notes:...}`
   - النتيجة: **REQ-20260729-T7K03 · id=13 · status=`submitted`** ✅ (محفوظ)
2. `admin@example.com` (أدمن، **نفس العيادة**) فتح `GET /approvals`:
   - `data.inventory_requests` = **`[]` فاضية** ❌

## دليل أنهما نفس العيادة
- `GET /manager/dashboard` (أدمن) و `GET /reception/dashboard` (رزبشن) يرجّعان **نفس الأرقام**:
  `patients_total = 68` و `appointments_today = 10`. → نفس العيادة قطعاً، فمو اختلاف عيادات.

## الأثر
- الطلب لا يصل الأدمن للاعتماد → **لا يقدر يوافق/يرفض** → وبالتالي **لا يصل إشعار للرزبشن**
  (رغم أن إشعارات الاعتماد الخمسة منشورة — لكنها لا تُستدعى لأن الطلب غير مرئي أصلاً).

## المطلوب فحصه بالباك‑إند
1. **الحالة:** هل استعلام `GET /approvals` للمخزون يفلتر على حالة معيّنة (مثلاً `pending`)
   بينما الطلب المُرسَل حالته **`submitted`**؟ وحّد الحالة المتوقعة.
2. **ربط العيادة:** في `/inventory/requests/mine` يظهر `clinic_id: null` — هل طلب المخزون
   يُنشأ **بدون clinic_id**؟ لو كذا، استعلام الأدمن المُقيّد بالعيادة لن يلتقطه.
   اربط الطلب بعيادة الرزبشن عند الإنشاء، أو صحّح فلتر الأدمن.

## اختبار القبول
```
رزبشن يرسل مخزون (submit:true) → أدمن نفس العيادة: GET /approvals → inventory_requests فيها الطلب
→ أدمن يرفض (PUT /approvals/inventory/{id}/reject {reason}) → الرزبشن يوصله إشعار بالسبب
```
> id=13 (REQ-20260729-T7K03) لا يزال `submitted` وغير مرئي للأدمن — استخدمه للفحص.
