# قائمة الباك-إند المتبقّية (مرتّبة بالأولوية) — لكلود الويب

كل نقطة مستقلة. ابدأ بـ P1 (سريعة + عالية الأثر). تفاصيل كل ميزة في ملفات
`PRODUCTION_HANDOFF_*.md` على فرع `maljaafari-work`.

---

## 🔴 P1 — سريعة وتفتح ميزات جاهزة في التطبيق

1. **حضور الرزبشن** — مسار واحد:
   `POST /api/reception/appointments/{appointment}/status`  body: `{ "status": "checked_in" | "no_show" }`
   يحدّث حالة الموعد. (التطبيق: أزرار حضر/غياب جاهزة.)

2. **ملف المريض للرزبشن (#7)** — مسار واحد:
   `GET /api/reception/patients/{patient}` → بيانات + مواعيد + `sessions_summary{total,completed,remaining}`
   التفاصيل: `PRODUCTION_HANDOFF_RECEPTION_PATIENT.md`.

3. **مستقبِلو الإشعارات** (بنفس الـhelper الموجود، غيّر قائمة المستقبِلين حسب الجدول):
   | الحدث | المستقبِلون |
   |---|---|
   | ملاحظة / سلفة (loan) | الأدمن فقط |
   | إجازة | الأدمن + الرزبشن |
   | نقل مريض | الأدمن + الرزبشن + `to_specialist_id` |
   | رزبشن → موظف معيّن | الموظف + الأدمن |
   | رزبشن → الكل | الكل |
   > المُرسِل دائماً مستثنى. `patient_transfer` الآن يصله `patient_id` + `to_specialist_id` في `POST /employee/requests`.

---

## 🟠 P2 — مهمة

4. **قبول/رفض الإجازة أو السلفة** → أرسل إشعار **للموظف صاحب الطلب** بالنتيجة (مقبول/مرفوض).

5. **تسجيل المصاريف (#9)** — مساران:
   `GET /api/admin/expense-categories` (قائمة التصنيفات) +
   `POST /api/admin/expenses` (multipart مع `receipt` صورة الفاتورة) → يترحّل مثل اللوحة.
   التفاصيل: `PRODUCTION_HANDOFF_EXPENSES.md`.
   > بدونه: صفحة المصروف بالتطبيق تطلع التصنيف فاضي.

6. **تأكيد إشعار طلب المخزون** يوصل الأدمن فعلاً (ذكرت إنه اتضاف — تأكّد إنه اننشر/يشتغل).

---

## 🟡 P3 — الأكبر

7. **ترجمة كل الإشعارات حسب لغة المستقبِل** — لا زالت بعض الإشعارات إنجليزية.
   التطبيق يرسل `locale` مع `register-token` ويُخزَّن على المستخدم. استخدمه لاختيار نص
   العنوان/المتن (عربي/إنجليزي) لكل أنواع الإشعارات.

8. **(#5) تعبئة `name_ar` تلقائياً** للمرضى اللي اسمهم القديم عربي (الأداة اللي عرضتها) — بضغطة.

---

## ملاحظات عامة
- كل ما سبق **بدون migrations جديدة** (عدا ما تحتاجه أنت داخلياً).
- ملفات التفاصيل على الفرع: `PRODUCTION_HANDOFF_RECEPTION_PATIENT.md`, `PRODUCTION_HANDOFF_EXPENSES.md`,
  `PRODUCTION_HANDOFF_PHYSICAL_EXAM.md`, `PRODUCTION_HANDOFF_PATIENT_REPORTS.md`, `PRODUCTION_HANDOFF_NOTE_ATTACHMENTS.md`.
- لا تغيّر أسماء حقول الردود الموجودة — التطبيق يعتمدها.
