# ملف تسليم: المساعد الذكي «د. يوسف» (للأخصائي)

مساعد ذكاء اصطناعي **داخلي للأخصائيين فقط** في مركز علاج طبيعي. يعتمد على **Claude API**
(المفتاح يبقى بالسيرفر). الواجهة تُبنى في التطبيق (فرع `maljaafari-work`)؛ المطلوب منك نقطة
اتصال واحدة تتكفّل بالاستدعاء وتجميع سياق المريض.

## ⚠️ مبادئ إلزامية (ضعها في system prompt)
- الاسم: **«د. يوسف»**، مساعد **علاج طبيعي وتأهيل** للأخصائيين.
- **يخاطب الأخصائي باسمه**: أدرج **اسم المستخدم المسجّل** (من التوكن) في تعليمات النظام،
  وليحيِّه/يخاطبه باسمه بشكل طبيعي (مثلاً «أهلاً د. صفاء، ...»)، بدون تكرار مبالغ فيه.
- **أداة مساعدة — القرار النهائي دائماً للأخصائي.** لا يقدّم تشخيصاً قاطعاً ولا يستبدل الحكم السريري.
- يرد **بالعربية** (أو بلغة الأخصائي)، بأسلوب مهني موجز.
- محصور في مجال العلاج الطبيعي/التأهيل؛ خارج المجال يعتذر بلطف.
- لا يخترع بيانات مريض؛ يعتمد فقط على السياق المُرسل.

## 0) قاعدة معرفة قابلة للتعديل (تعليمات المركز) — مهمة
اجعل النظام يقرأ **تعليمات مخصّصة للعيادة** ويحقنها في system prompt لكل طلب، بحيث
يعرف «د. يوسف» شركتهم ويلتزم بقواعدهم:
- خزّن نصاً واحداً قابلاً للتعديل (مثلاً حقل `ai_assistant_instructions` على مستوى العيادة).
- **يُعدّله الأدمن من لوحة التحكم** (لا يُكتب بالكود) — أضِف حقل تحرير في اللوحة.
- يحوي: تعريف المركز وخدماته، القواعد الإلزامية (مثلاً «ذكّر باستشارة الطبيب المعالج»،
  «لا تقترح أدوية»، الوحدات، النبرة)، والبروتوكولات المفضّلة.
- **يُدمج دائماً** ضمن تعليمات النظام قبل رسائل المحادثة، فوق المبادئ الإلزامية أدناه.
> النتيجة: مساعد مخصّص للمركز، يتحدّث بقواعدهم، ويتحدّث بدون تحديث تطبيق عند تغيير التعليمات.

## 0.1) تسجيل المحادثات ورقابة الأدمن
- **سجّل كل محادثة** مع «د. يوسف»: `employee_id`, `clinic_id`, الرسائل (user/assistant),
  `patient_id` إن وُجد، الطابع الزمني.
- **صفحة للأدمن في لوحة التحكم**: قائمة المحادثات لكل موظف، مع إمكانية فتح نص أي محادثة.
  (مثلاً `GET /admin/ai/conversations?employee_id=`.)
  (التسجيل يتم بصمت من جانب السيرفر — بدون تنويه في واجهة الموظف حسب رغبة العميل.)

## 1) نقطة الاتصال
```
POST /api/ai/assistant     (صلاحية: أخصائي مسجّل الدخول)
```
### الطلب
```json
{
  "messages": [
    { "role": "user", "content": "وش تمارين تأهيل الرباط الصليبي الأمامي؟" },
    { "role": "assistant", "content": "..." }
  ],
  "patient_id": 12,          // اختياري — لربط سياق مريض
  "action": "chat"           // chat | summarize_patient | draft_note (تلميح للغرض)
}
```
### الرد
```json
{ "data": { "reply": "نص رد المساعد..." } }
```
> يكفي رد نصّي واحد. (اختياري لاحقاً: بث SSE للكتابة الحيّة — راجع القسم الأخير.)

## 2) ربط سياق المريض (لما `patient_id` موجود)
- **تحقّق أن المريض ضمن مرضى هذا الأخصائي / عيادته** — وإلا 403. (خصوصية إلزامية.)
- جمّع ملخّصاً مختصراً وأدرجه في سياق النظام قبل استدعاء Claude:
  - الاسم، الجنس، العمر (إن وُجد)
  - الباقة/الجلسات (إجمالي/مكتمل/متبقّي)
  - آخر جلسات وملاحظاتها، التشخيص/الحالة إن وُجد، التقدّم
- **لا ترسل بيانات مرضى آخرين.** أرسل فقط ما يلزم للمهمة.

## 3) الأفعال (action)
- `chat`: سؤال مرجعي عام في العلاج الطبيعي.
- `summarize_patient`: لخّص حالة وتقدّم المريض المرتبط (يتطلب `patient_id`).
- `draft_note`: اكتب مسودة تقرير/ملاحظة جلسة احترافية من النقاط التي يعطيها الأخصائي (+ سياق المريض إن وُجد).

## 4) النموذج والتكلفة
- ابدأ بموديل سريع/اقتصادي (مثلاً Haiku) للمحادثة العامة، وارفع لموديل أقوى للتلخيص/الصياغة إن لزم.
- حدّد سقفاً معقولاً لطول المحادثة/التوكنز لضبط التكلفة.
- راجع مهارة `claude-api` عندك لأحدث معرّفات الموديلات والاستخدام.

## اختبار القبول
```
POST /ai/assistant {messages:[{role:user,content:"..."}]}          → 200، data.reply نص مفيد بالعربي
POST /ai/assistant {..., patient_id:<مريض الأخصائي>, action:"summarize_patient"} → ملخّص دقيق للمريض
POST /ai/assistant {..., patient_id:<مريض أخصائي آخر>}             → 403 (خصوصية)
```

## (اختياري لاحقاً) البث الحيّ
لو حبيت تجربة كتابة حيّة: `POST /ai/assistant/stream` يرجّع `text/event-stream`
(أحداث `data: {"delta":"..."}` ثم `data: [DONE]`). التطبيق جاهز يدعمه لاحقاً؛ غير مطلوب للنسخة الأولى.
