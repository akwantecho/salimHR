# ملف تسليم: حذف ملاحظة موظف (لتنظيف صندوق الأدمن)

الأدمن يبي يحذف ملاحظات الموظفين المتراكمة. الواجهة جاهزة (أيقونة سلة على كل ملاحظة +
تأكيد) على فرع `maljaafari-work`. المطلوب مسار واحد:

```php
DELETE /api/manager/notes/{note}
```
- يحذف الملاحظة (وردودها/مرفقاتها) نهائياً. صلاحية الأدمن/المدير فقط.
- الرد: `2xx` (التطبيق يزيلها تفاؤلياً ويعيدها لو فشل).

> حالياً `DELETE /manager/notes/{id}` غير موجود (404 route not found).
> موجود فقط: `GET /manager/notes` و `POST /manager/notes/{id}/reply`.

## اختبار
```
DELETE /manager/notes/{id} → 2xx، والملاحظة تختفي من GET /manager/notes
```
