# طلب صغير: أضِف room_number لبيانات الموعد في رد الأدمن

**السياق:** الأخصائي يختار الغرفة في التطبيق وتُحفظ عبر `POST /appointments/{id}/room`
(`room_number` مخزّن في سجل الموعد). الأدمن يبي يشوف الغرفة **كمعلومة للقراءة فقط**.

**المشكلة:** رد `GET /api/manager/appointments` (وأي مسار مواعيد للأدمن/الرزبشن) **ما يتضمّن**
`room_number` ضمن كائن الموعد — مع إن القيمة موجودة في القاعدة.

**المطلوب:** أضِف الحقل للـ resource/الرد:
```json
{ "id": 12, "start_time": "...", "status": "...", "patient": {...},
  "specialist": {...}, "service": {...},
  "room_number": 3 }        // ← أضِف هذا (int أو null)
```
- يكفي إضافته في `/manager/appointments` (وحبّذا `/reception/*` المواعيد و
  `reception/patients/{id}.appointments` للاتساق).
- التطبيق **جاهز**: يقرأ `room_number` ويعرضه كشارة «غرفة N» للقراءة فقط. null → ما يظهر شي.

**اختبار:** موعد له غرفة مختارة → الرد يحوي `room_number` → تظهر الشارة عند الأدمن.
